# Batch Report — 2026-05-01

## Phase 14 — Frontend Feature Completeness + Design Upgrade (T113–T124)

### Features Implemented

| Task | Feature | Description |
|------|---------|-------------|
| T113 | Linkable lightbox | `?photo=<id>` URL param syncs with lightbox. Shared `useLightboxNav` hook used by GalleryPage, FavoritesPage, AlbumPage |
| T114 | Inline editing | Owner-only edit panel in lightbox: title, description, album select, tag chips + new tag creation input |
| T115 | Upload metadata | Album select + tag chips in UploadModal, passed as `meta` to upload API |
| T116 | Favorites-first sort | 5th sort option added to Navbar dropdown |
| T117 | Album CRUD | Create (name + description), rename (inline), delete (ConfirmDialog) — all in Sidebar |
| T118 | Multi-select + bulk delete | Select mode on GalleryPage with checkbox overlays + bulk delete with ConfirmDialog |
| T119 | Tag creation | + button in sidebar opens inline input, Enter creates slug and toggles as filter |
| T120 | Stats bar | Photo/album/tag counts with icons at bottom of sidebar |
| T121 | Empty state + /register | Icon + CTA button on empty gallery; `/register` route alias |
| T122 | Design upgrade | Dark sidebar, brand palette, Pinterest-style PhotoCard, compact Navbar, ConfirmDialog component |

### Bug Fixes

| Task | Bug | Root Cause | Fix |
|------|-----|-----------|-----|
| T123 | Sort yields zero results | Navbar sent UI values (`newest`, `title_asc`) as `sort` param; backend expects `created_at`, `title`, `favorites` | Added mapping: each dropdown option → correct `sort` + `order` params, plus reverse mapping for display |
| T124 | Select mode clicks do nothing | `renderOverlay` div (absolute, inset-0) sat on top of PhotoCard and intercepted clicks with no handler | MasonryGrid wraps overlay in a clickable div with `onClick` that fires `onClick?.(photo)` |
| — | Browser `window.confirm` alerts | Used native browser dialogs for delete confirmations | Created `ConfirmDialog` component with icon, styled buttons, proper focus management |

### Files Created

| File | Purpose |
|------|---------|
| `src/hooks/useLightboxNav.ts` | Shared hook: syncs lightbox open/navigate/close with `?photo=` URL param |
| `src/components/ConfirmDialog.tsx` | Reusable styled confirmation dialog (replaces `window.confirm`) |

### Files Modified

| File | Changes |
|------|---------|
| `src/index.css` | Dark sidebar color tokens, brand palette (50–700), accent color, surface bg |
| `src/data/copy.ts` | New strings: stats, gallery select/delete, album description, tag creation hints, confirm dialog |
| `src/components/Shell.tsx` | `bg-surface` on root container |
| `src/components/Navbar.tsx` | Compact h-14, brand icon (Camera), avatar circle, sort → backend param mapping |
| `src/components/Sidebar.tsx` | Dark theme, album create with description, inline rename, ConfirmDialog for delete, tag creation input, stats bar |
| `src/components/MasonryGrid.tsx` | 5 columns on xl, overlay wrapped in clickable div, increased gap |
| `src/components/PhotoCard.tsx` | Pinterest-style: rounded-2xl, hover shadow+scale, gradient overlay, slide-up info bar with album name |
| `src/components/PhotoLightbox.tsx` | Edit panel with tag creation input, owner-only delete button, keyboard shortcuts disabled in edit mode |
| `src/components/UploadModal.tsx` | Album select + tag chips before upload |
| `src/pages/GalleryPage.tsx` | Multi-select mode, bulk delete with ConfirmDialog, improved empty state |
| `src/pages/FavoritesPage.tsx` | Uses `useLightboxNav`, overlay click fix |
| `src/pages/AlbumPage.tsx` | Editable description (owner-only), uses `useLightboxNav` |
| `src/App.tsx` | Added `/register` route |
| `src/api/photos.ts` | Added `new_tags` to `UpdatePhotoPayload` |
| `src/hooks/useUploadFlow.ts` | Accepts `meta` parameter for album/tags |

### Documentation Updated

| File | Changes |
|------|---------|
| `docs/REQUIREMENTS.md` | Updated F1, F2, F3, F4, F6, F7 feature catalogs + added US-25a, US-26a user stories |
| `docs/TASKS.md` | Added Phase 14 (T113–T124), updated task summary table (124 total tasks) |

---

## Test Results

**Backend:** 190 passed, 1 skipped (3.0s)
**Frontend:** 18 passed (0.9s)
**Build:** 107 KB gzip (budget: ≤200 KB)
**TypeScript:** 0 errors
**ESLint:** 0 errors
